{
  "name": "@sqb/mysql",
  "description": "SQB adapter for the mysql2 driver",
  "version": "6.0.9",
  "author": "Panates",
  "license": "BSD-3-Clause",
  "homepage": "https://www.sqbjs.org",
  "scripts": {
    "compile": "tsc --noEmit",
    "prebuild": "npm run check && npm run lint && npm run clean",
    "build": "tsc -b tsconfig-build.json",
    "postbuild": "node ../../support/postbuild.cjs",
    "lint": "eslint . --max-warnings=0",
    "lint:fix": "eslint . --max-warnings=0 --fix",
    "format": "prettier . --write --log-level=warn",
    "check": "dpdm -T --no-warning --no-tree --skip-dynamic-imports circular --exit-code circular:1 ./src/index.ts",
    "test": "mocha",
    "clean": "npm run clean:src && npm run clean:test && npm run clean:build && npm run clean:cover",
    "clean:src": "ts-cleanup -s src --all",
    "clean:test": "ts-cleanup -s test --all",
    "clean:build": "rimraf build *.tsbuildinfo",
    "clean:cover": "rimraf ../../coverage/mysql"
  },
  "dependencies": {
    "tslib": "^2.8.1"
  },
  "devDependencies": {
    "expect": "^30.5.1",
    "mysql2": "^3.24.4"
  },
  "peerDependencies": {
    "@sqb/builder": "^6.0.9",
    "@sqb/connect": "^6.0.9",
    "@sqb/mysql-dialect": "^6.0.9",
    "mysql2": "^3.24.4"
  },
  "type": "module",
  "module": "./index.js",
  "types": "./index.d.ts",
  "exports": {
    ".": {
      "types": "./index.d.ts",
      "default": "./index.js"
    },
    "./package.json": "./package.json"
  },
  "engines": {
    "node": ">=20.0"
  },
  "contributors": [
    "Eray Hanoglu <e.hanoglu@panates.com>",
    "Ilker Gurelli <i.gurelli@panates.com>"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/panates/sqb.git",
    "directory": "packages/mysql"
  },
  "keywords": [
    "sqb",
    "mysql",
    "mysql2",
    "sql",
    "query",
    "builder",
    "dialect",
    "serializer",
    "database"
  ],
  "publishConfig": {
    "access": "public"
  }
}