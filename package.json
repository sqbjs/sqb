{
  "name": "sqb",
  "description": "Extensible, multi-dialect SQL query builder and Database connection framework for JavaScript",
  "version": "3.3.0",
  "author": "Panates Ltd.",
  "contributors": [
    "Eray Hanoglu <e.hanoglu@panates.com>"
  ],
  "license": "MIT",
  "repository": "panates/sqb",
  "main": "lib/index.js",
  "keywords": [
    "javascript",
    "sql",
    "query",
    "builder",
    "dialect",
    "multi-dialect",
    "database"
  ],
  "dependencies": {
    "debug": "^3.1.0",
    "doublylinked": "^2.0.0",
    "errorex": "^1.0.6",
    "lightning-pool": "^2.0.2",
    "putil-flattentext": "^2.0.0",
    "putil-isplainobject": "^1.0.1",
    "putil-merge": "^2.0.1",
    "putil-promisify": "^1.2.0",
    "putil-taskqueue": "^2.0.1",
    "putil-waterfall": "^2.0.2"
  },
  "devDependencies": {
    "babel-eslint": "^9.0.0",
    "eslint": "^5.5.0",
    "eslint-config-google": "^0.10.0",
    "istanbul": "^0.4.5",
    "mocha": "^5.2.0"
  },
  "engines": {
    "node": ">= 6.0"
  },
  "directories": {
    "lib": "./lib"
  },
  "files": [
    "LICENSE",
    "README.md",
    "lib/"
  ],
  "scripts": {
    "test": "mocha --require test/support/env --reporter spec --bail --check-leaks test/",
    "test-cov": "istanbul cover node_modules/mocha/bin/_mocha -- -R spec --require test/support/env --reporter dot --check-leaks test/",
    "test-travis": "istanbul cover node_modules/mocha/bin/_mocha --report lcovonly -- -R spec --require test/support/env --reporter spec --check-leaks test/"
  }
}
